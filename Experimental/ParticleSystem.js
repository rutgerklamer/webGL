var ParticleSystem = function()
{
  Mesh.call(this);
  this.particles = new Array();
  this.SpawnParticle = function(amount)
  {
  }
  this.Update()
  {
    for (i = 0; i < particles.length; i++)
    {
      particles.position
    }
  }
}
